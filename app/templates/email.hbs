<h1>{{inboxName}}</h1>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Time</th>
            <th>Subject</th>
        </tr>
    </thead>
    <tbody>
    {{#each}}
        <tr>
            <td>
                {{format-date created_at}}
            </td>
            <td>
                {{#link-to "email" controller.inboxName}}
                    {{subject}}
                {{/link-to}}
            </td>
        </tr>
    {{/each}}
    </tbody>
</table>
